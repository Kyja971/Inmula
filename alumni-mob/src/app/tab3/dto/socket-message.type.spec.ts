import { SocketMessageType } from './socket-message.type';

describe('SocketMessageType', () => {
  it('should create an instance', () => {
    expect(new SocketMessageType()).toBeTruthy();
  });
});
